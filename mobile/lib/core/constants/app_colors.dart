import 'package:flutter/material.dart';

class AppColors {
  // Palette "Zen Dark"
  static const Color background = Color(0xFF121212); // OLED Black
  static const Color surface = Color(0xFF1E1E1E);
  
  // Accents Néon
  static const Color primary = Color(0xFF2979FF); // Electric Blue (Work)
  static const Color secondary = Color(0xFF00E676); // Mint Green (Personal)
  static const Color error = Color(0xFFFF5252); // Soft Red (Urgent)
  static const Color success = Color(0xFF4CAF50); // Green (Success)
  
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFB0BEC5);
}
